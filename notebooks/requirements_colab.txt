# SmarTRIZ Colab Requirements
# Install in this order to avoid version conflicts on Colab's base environment.
# Paste into a cell: !pip install -r requirements_colab.txt

# Core HuggingFace stack (pin together)
transformers==4.45.2
datasets==3.0.1
accelerate==0.34.2
tokenizers==0.20.3

# PEFT + training
peft==0.13.2
trl==0.11.4

# Quantization (requires CUDA — skip on CPU-only machines)
bitsandbytes==0.44.1

# API client (DeepInfra teacher model access)
openai>=1.40.0

# Experiment tracking (Notebook 01)
wandb>=0.17.0

# Evaluation metrics (Notebook 03)
sentence-transformers>=3.0.0
scikit-learn>=1.3.0

# Visualization (Notebook 00)
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.66.0
numpy>=1.26.0
